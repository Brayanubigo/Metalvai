{
    "builds": [{
        "src": "./index.js",
        "use": "@vercel/nodemon"
    }],
    "routes": [{
        "src":"./(.*)",
        "des":"/"
    }],
    "rewrites": 
    [{ "source": "/src/(.*)",
     "destination": "/" }]
}